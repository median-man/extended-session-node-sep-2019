# extended-session-node-sep-2019

Use my-bot to lookup song information on Spotify from the convenience of your
favorite terminal.

## Setup

Add Spotify keys to `.env.example` and rename the file as `.env`.
